const COLOR = {
  MAIN: '#CFD3CE',
};

const VIEWSIZE = {
  REACTWIDTH: 1000
};

const STATE = {
  DEFAULT: Symbol(),
  TICKETSIGN: Symbol(),
  TICKETMODAL: Symbol(),
  SELECT: Symbol(),
}

const EVENT = {
  LOAD : Symbol(),
  TICKETUI : Symbol(),
}

export { COLOR, VIEWSIZE, STATE, EVENT };

